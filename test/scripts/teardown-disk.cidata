#!/usr/bin/env bash

set -euo pipefail

VOL_NAME="$(basename "${CIDATA_IMAGE}")"
POOL_NAME="$(basename "$(dirname "${CIDATA_IMAGE}")")"

virsh vol-delete "${VOL_NAME%.*}.sha256sum" --pool "${POOL_NAME}" || true
virsh vol-delete "${VOL_NAME}" --pool "${POOL_NAME}"
